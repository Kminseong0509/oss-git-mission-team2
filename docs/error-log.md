# Error Log

## 1. 충돌 해결 기록

### 상황
팀 프로젝트에서 여러 명이 같은 파일을 수정하면 merge 과정에서 충돌이 발생할 수 있다.

### 원인
같은 파일의 같은 부분을 서로 다른 브랜치에서 수정했기 때문에 Git이 어떤 내용을 선택해야 할지 자동으로 판단하지 못했을 가능성이 있다.

### 해결 방법
충돌이 발생한 파일을 열어 `<<<<<<<`, `=======`, `>>>>>>>` 표시를 확인한 뒤 필요한 내용을 남기고 나머지는 삭제하는 방법이 있다.

